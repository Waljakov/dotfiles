# This file has been auto-generated by i3-config-wizard(1).
# It will not be overwritten, so edit it as you like.
#
# Should you change your keyboard layout some time, delete
# this file and re-run i3-config-wizard(1).
#

# i3 config file (v4)
#
# Please see https://i3wm.org/docs/userguide.html for a complete reference!

set $mod Mod4
set $mod2 Mod1

# Execute on startup
exec_always feh --bg-center ~/.wallpaper/21-9_artistic_planet.jpg
exec_always optimus-manager-qt
exec_always start-pulseaudio-x11
exec_always compton
exec_always unclutter -idle 3
exec_always --no-startup-id sh ~/.config/polybar/launch.sh
exec_always "setxkbmap -layout us,de -option 'grp:alt_caps_toggle'"
#exec_always "setxkbmap -option 'grp:alt_shift_toggle'"
exec_always nm-applet
exec_always mpris-proxy
#exec redshift-gtk
# exec nextcloud
# exec wal -R
#exec kdeconnect-indicator


# Use Mouse+$mod to drag floating windows to their wanted position
floating_modifier $mod

default_orientation vertical #start in opposite orientation from your monitor
for_window [class=".*"] split toggle #toggles split at each new window

# Mouse behaviour yes|no ; output|none
focus_follows_mouse no
mouse_warping none

# start a terminal
bindsym $mod+Return exec i3-sensible-terminal
# bindsym $mod+Return exec fish

# Scratchpad calculator
for_window [title="floating_console"] floating enable
for_window [title="floating_console"] move scratchpad
exec_always --no-startup-id alacritty -t floating_console

# Scratchpad ranger
for_window [title="ranger_float"] floating enable
for_window [title="ranger_float"] move scratchpad
exec_always --no-startup-id alacritty -t ranger_float -e ranger
# increase width of ranger
for_window [title="ranger_float"] resize grow width 400 px

# Polybar popup-calendar
for_window [class="Yad" title="yad-calendar"] floating enable

#Scratchpad keepass
for_window [class="KeePass"] floating enable
for_window [class="KeePass"] move scratchpad
for_window [class="KeePass"] resize set height 841 px
exec_always --no-startup-id keepassxc

# kill focused window
bindsym $mod+Shift+q kill

# change focus
bindsym $mod+h focus left
bindsym $mod+j focus down
bindsym $mod+k focus up
bindsym $mod+l focus right

# alternatively, you can use the cursor keys:
bindsym $mod+Left focus left
bindsym $mod+Down focus down
bindsym $mod+Up focus up
bindsym $mod+Right focus right

# move focused window
bindsym $mod+Shift+h move left
bindsym $mod+Shift+j move down
bindsym $mod+Shift+k move up
bindsym $mod+Shift+l move right

# alternatively, you can use the cursor keys:
bindsym $mod+Shift+Left move left
bindsym $mod+Shift+Down move down
bindsym $mod+Shift+Up move up
bindsym $mod+Shift+Right move right

# split in horizontal orientation
bindsym $mod+b split h

# split in vertical orientation
bindsym $mod+v split v

# enter fullscreen mode for the focused container
bindsym $mod+f fullscreen toggle

# move to scratchpad
# view scratchpad
bindsym $mod2+Shift+space move scratchpad
bindsym $mod2+space scratchpad show

# change container layout (stacked, tabbed, toggle split)
# bindsym $mod+Shift+s layout stacking
bindsym $mod+t layout tabbed
bindsym $mod+Shift+t layout toggle split

# toggle tiling / floating
bindsym $mod+Shift+space floating toggle

# change focus between tiling / floating windows
bindsym $mod+space focus mode_toggle

# start lock screen
bindsym $mod+Ctrl+l exec i3lock -i ~/.wallpaper/jupiter.png -t
bindsym $mod+Ctrl+s exec shutdown now
bindsym $mod+Ctrl+r exec shutdown -r now

# focus the parent container
# bindsym $mod+a focus parent

# focus the child container
#bindsym $mod+d focus child

# monitors
set $Mon1 eDP-1-1
set $Mon2 DP-1

# Define names for default workspaces for which we configure key bindings later on.
# We use variables to avoid repeating the names in multiple places.
set $ws1 "1 "
set $ws2 "2 +"
set $ws3 "3 "
set $ws4 "4 "
set $ws5 "5 "
set $ws6 "6"
set $ws7 "7"
set $ws8 "8"
set $ws9 "9 "
set $ws10 "10 "

# workspace assignment
workspace $ws1 output $Mon1
workspace $ws2 output $Mon2
workspace $ws3 output $Mon1
workspace $ws4 output $Mon1
workspace $ws5 output $Mon2
workspace $ws6 output $Mon2
workspace $ws7 output $Mon2
workspace $ws8 output $Mon2
workspace $ws9 output $Mon2
workspace $ws10 output $Mon2
# switch to workspace
bindsym $mod+1 workspace $ws1
bindsym $mod+2 workspace $ws2
bindsym $mod+3 workspace $ws3
bindsym $mod+4 workspace $ws4
bindsym $mod+5 workspace $ws5
bindsym $mod+6 workspace $ws6
bindsym $mod+7 workspace $ws7
bindsym $mod+8 workspace $ws8
bindsym $mod+9 workspace $ws9
bindsym $mod+0 workspace $ws10

# move focused container to workspace
bindsym $mod+Shift+1 move container to workspace $ws1
bindsym $mod+Shift+2 move container to workspace $ws2
bindsym $mod+Shift+3 move container to workspace $ws3
bindsym $mod+Shift+4 move container to workspace $ws4
bindsym $mod+Shift+5 move container to workspace $ws5
bindsym $mod+Shift+6 move container to workspace $ws6
bindsym $mod+Shift+7 move container to workspace $ws7
bindsym $mod+Shift+8 move container to workspace $ws8
bindsym $mod+Shift+9 move container to workspace $ws9
bindsym $mod+Shift+0 move container to workspace $ws10

# Assign Programs to Workspaces
assign [class="Steam"] $ws10
assign [class="Thunderbird"] $ws3
for_window [class="Spotify"] move to workspace $ws9

# Set Colors
set                             $transparent                    #00000000
set                             $black                          #000000
set_from_resource               $bg-color                       i3wm.background
set_from_resource               $inactive-bg-color              i3wm.color8 #2e2e2e
set_from_resource               $text-color                     i3wm.foreground #ff5c33
set_from_resource               $inactive-text-color            i3wm.color15 #e0e0e0
set_from_resource               $urgent-bg-color                i3wm.color9 #ba3b46
set_from_resource               $urgent-color                   i3wm.color1 #ba3b46
set_from_resource               $primary-color                  i3wm.color4 #ba3b46
set_from_resource               $secondary-color                i3wm.color12 #ba3b46

# show borders only when >1 window on workspace
smart_borders on

# window colors
#                       border               background         text                 indicator                          child_border
client.focused          $primary-color       $text-color        $text-color          $secondary-color                   $primary-color
client.unfocused        $transparent         $transparent       $inactive-text-color $transparent                       $transparent
client.focused_inactive $transparent         $transparent       $inactive-text-color $transparent                       $transparent
client.urgent           $urgent-bg-color     $urgent-bg-color   $text-color          $urgent-color                      $urgent-bg-color

# reload the configuration file
bindsym $mod+Shift+c reload
# restart i3 inplace (preserves your layout/session, can be used to upgrade i3)
bindsym $mod+Shift+r restart
# exit i3 (logs you out of your X session)
bindsym $mod+Shift+x exec "i3-nagbar -t warning -m 'You pressed the exit shortcut. Do you really want to exit i3? This will end your X session.' -B 'Yes, exit i3' 'i3-msg exit'"

# resize window (you can also use the mouse for that)
mode "resize" {
        # These bindings trigger as soon as you enter the resize mode

        bindsym h resize shrink width 10 px or 10 ppt
        bindsym j resize grow height 10 px or 10 ppt
        bindsym k resize shrink height 10 px or 10 ppt
        bindsym l resize grow width 10 px or 10 ppt

        # same bindings, but for the arrow keys
        bindsym Left resize shrink width 10 px or 10 ppt
        bindsym Down resize grow height 10 px or 10 ppt
        bindsym Up resize shrink height 10 px or 10 ppt
        bindsym Right resize grow width 10 px or 10 ppt

        # Gap sizes
        bindsym Shift+h gaps outer current plus 10
        bindsym Shift+l gaps outer current minus 10
        bindsym Shift+j gaps inner current plus 5
        bindsym Shift+k gaps inner current minus 5

        # center
        bindsym Shift+c gaps horizontal current toggle 300

        # back to normal: Enter or Escape or $mod+r
        bindsym Return mode "default"
        bindsym Escape mode "default"
        bindsym $mod+r mode "default"

        # Laptop + Screen above without overlap
        bindsym Shift+d gaps bottom current toggle 200
}

bindsym $mod+r mode "resize"

# Gap settings
gaps inner 10
gaps outer -10
# i3-gaps dont work with titlebars
for_window [class="^.*"] border pixel 1
# default_border pixel 1 # Steam has no border

# Media player controls
# bindsym XF86AudioPlay exec playerctl play
# bindsym XF86AudioPause exec playerctl pause
# bindsym XF86AudioNext exec playerctl next
# bindsym XF86AudioPrev exec playerctl previous

# Volume Key-bindings
set $sink `pactl list short sinks | grep RUNNING | cut -f1`
bindsym XF86AudioMute exec pactl set-sink-mute $sink toggle
bindsym XF86AudioLowerVolume exec pactl set-sink-volume $sink -5%
bindsym XF86AudioRaiseVolume exec pactl set-sink-volume $sink +5%

# Screen Brightness
bindsym XF86MonBrightnessUp exec brightnessctl s +2%
bindsym XF86MonBrightnessDown exec brightnessctl s 2%-

# Launch Programs
bindsym $mod+w exec firefox
bindsym $mod+s exec steam
bindsym $mod+Shift+s exec spotify
bindsym $mod+m exec thunderbird
bindsym $mod+c [title="floating_console"] scratchpad show; [title="floating_console"] move position center
bindsym $mod+Shift+f [title="ranger_float"] scratchpad show; [title="ranger_float"] move position center
bindsym $mod+y [class="KeePass"] scratchpad show; [class="KeePass"] move position center
bindsym $mod+z [class="KeePass"] scratchpad show; [class="KeePass"] move position center

bindsym $mod+d exec rofi -show run -theme my_dmenu
bindsym $mod+Shift+d exec sudo rofi -show run -theme my_dmenu

bindsym $mod+p exec rofi  -show fb -modi fb:/home/michar/.config/rofi/rofi-file-browser.sh -theme file_browser
